#pragma once

#import <Cocoa/Cocoa.h>

/// Application lifecycle: builds the main menu, opens the main window and
/// terminates the process when that window closes.
@interface ArTermAppDelegate : NSObject <NSApplicationDelegate>
@end
